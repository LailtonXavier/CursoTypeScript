{
  "code-runner.executorMap": {
    // "typescript": "npx ts-node --files",
    // clear == ele limpa o terminal e depois roda (lini/mac)
    // cls == ele limpa o terminal e depois roda (windows)

    // "typescript": "clear && npx ts-node --files",
    "typescript": "clear && npx ts-node --files --transpile-only",
  }
}
